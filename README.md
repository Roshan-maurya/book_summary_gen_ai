# book_summary_gen_ai
A Flask-based backend application that allows admin users to add books and automatically generates AI-based summaries asynchronously using a background thread. The application uses PostgreSQL for persistence, JWT-based authentication, and role-based access control.
